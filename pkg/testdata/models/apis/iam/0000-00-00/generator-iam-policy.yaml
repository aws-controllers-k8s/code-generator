ignore:
  resource_names:
   - AccessKey
   - AccountAlias
   - Group
   - InstanceProfile
   - LoginProfile
   - OpenIDConnectProvider
   # - Policy
   - PolicyVersion
   # - Role
   - SAMLProvider
   - ServiceLinkedRole
   - ServiceSpecificCredential
   - User
   - VirtualMFADevice
resources:
  Role:
    fields:
      AssumeRolePolicyDocument:
        is_iam_policy: true
  Policy:
    fields:
      PolicyDocument:
        is_iam_policy: true
